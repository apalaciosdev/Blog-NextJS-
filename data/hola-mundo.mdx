---
title: 'Primer blog en nextJS'
date: '2021-11-22'
---

# Hola mundo

Sis gg:
<CodeVisualizer language="js" code="
export default function CodeVisualizer({ code, language }) {
  useEffect(() => {
    Prism.manual = true;
    Prism.highlightAll();
  }, []);
  return (
    <div className=code>
      <pre className='code code-css'>
        <code className={`language-${language}`}>{code}</code>
      </pre>
    </div>
  );
}
"
  
/>